import { FiltroPropiedadesPipe } from './filtro-propiedades.pipe';

describe('FiltroPropiedadesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPropiedadesPipe();
    expect(pipe).toBeTruthy();
  });
});
